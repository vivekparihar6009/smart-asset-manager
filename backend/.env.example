# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_asset_manager

# Alternate Database URL (Used by Docker Compose or Cloud deployment)
# DATABASE_URL=postgres://postgres:postgres@db:5432/smart_asset_manager

# Security Credentials
JWT_SECRET=super_secret_key_change_me_in_production_987654321
JWT_EXPIRES_IN=24h
